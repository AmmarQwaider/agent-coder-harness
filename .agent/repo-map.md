# Repository Map

This document outlines the high-level architecture of the repository to help you navigate efficiently without needing to index the entire directory structure.

## Overview
- **Backend**: (e.g., `src/api`, `src/services`, `src/models`) - Contains the core business logic, API routes, and database interactions.
- **Frontend**: (e.g., `src/client`, `src/components`, `src/pages`) - Contains the UI components, state management, and client-side logic.
- **Database**: (e.g., `db/migrations`, `db/seeds`) - Contains database schemas, migrations, and seed data.
- **Config**: (e.g., `config/`, `.env.example`) - Environment configurations and setup files.
- **Tests**: (e.g., `tests/`, `spec/`) - Unit, integration, and end-to-end tests.

*(Note: Replace the above placeholders with the actual directory structure once the project is initialized.)*
